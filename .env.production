# Production build env vars — committed to the repo and read by Vite during
# `pnpm build`. Cloudflare Workers Builds picks this up automatically on
# every push because the file is on disk in the CI checkout.
#
# Only public values belong here. Anything secret (e.g. OPENAI_API_KEY) goes
# in the Cloudflare dashboard as a Worker Secret — never in this file.

# Google Drive integration — browser-only PKCE; values are public.
# See CLAUDE.md → External integrations for how to obtain these in GCP.
VITE_GOOGLE_CLIENT_ID=930960352387-h1qa6ppt0utvapv632vvq6nh4lgj5q2d.apps.googleusercontent.com
VITE_GOOGLE_API_KEY=AIzaSyCgWlI7LIJOIbF5V46QgIv8K-RqwS0HcQg
VITE_GOOGLE_PICKER_APP_ID=930960352387

